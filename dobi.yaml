# ===================================================
# alias
# ===================================================

alias=build:
  tasks:
    - cplusplus-container-images-builder-build
    - cplusplus-container-images-runtime-build
  annotations:
    description: "-> build all images"

alias=deploy:
  tasks:
    - cplusplus-container-images-builder-deploy
    - cplusplus-container-images-runtime-deploy
  annotations:
    description: "-> deploy all images"
