# ===================================================
# mount points
# ===================================================

mount=mount-cplusplus-container-images-gitversion-source-dir:
  bind: "{fs.projectdir}"
  path: /git
  read-only: false # needs to be read/write to work correctly!

mount=mount-cplusplus-container-images-gitversion-gen-dir:
  bind: "{fs.projectdir}/gen/gitversion"
  path: /gen
  read-only: false

# ===================================================
# images
# ===================================================

image=image-cplusplus-container-images-gitversion:
  image: elbb/bb-gitversion
  tags: ["0.6.2"]
  pull: once

# ===================================================
# jobs
# ===================================================

job=cplusplus-container-images-version:
  use: image-cplusplus-container-images-gitversion
  mounts:
    - mount-cplusplus-container-images-gitversion-source-dir
    - mount-cplusplus-container-images-gitversion-gen-dir
  sources: "./"
  artifact: "gen/gitversion/"
  env:
    - "USERID={user.uid}"
